// Auto-generated by parse-ts.js. Do not edit manually.
import express, { type Request, type Response } from 'express'
import cors from 'cors'
import rateLimit from 'express-rate-limit'
import helmet from 'helmet'
import { decode, encode } from '@msgpack/msgpack'
import { writeLog } from './logger'
{{{importsLine}}}

const errorStatusCodes = {{{errorStatusCodes}}}

const app = express()
app.use(helmet())

{{{rawCode}}}

{{{routesCode}}}

const port = Number(process.env.PORT ?? 3000)

app.listen(port, async () => {
    await writeLog('INFO', `Generated server listening on port ${port}`)
}).on('error', async (error) => {
    await writeLog('ERROR', `Failed to start server on port ${port}`, { error: error.message })
    process.exit(1)
})

export default app
