const missingParams = [{{{paramList}}}].filter((name) => !(name in args))

if (missingParams.length > 0) {
    await writeLog('WARN', 'Missing required params', { method: req.method, path: req.path, missingParams })
    res.status(400).json({ error: 'Missing params', missingParams })
    return
}
