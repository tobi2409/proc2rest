// Auto-generated by parse-ts.js. Do not edit manually.
import express, { type Request, type Response } from 'express'
import cors from 'cors'
import rateLimit from 'express-rate-limit'
import { decode, encode } from '@msgpack/msgpack'
import { writeLog } from './logger.generated'
{{importsLine}}

const app = express()
app.use(cors({{corsOptions}}))
app.use(rateLimit({{rateLimitOptions}}))

{{rawCode}}

{{routesCode}}

const port = Number(process.env.PORT ?? 3000)

app.listen(port, () => {
	writeLog('INFO', `Generated server listening on port ${port}`)
}).on('error', (error) => {
	writeLog('ERROR', `Failed to start server on port ${port}`, { error: error.message })
	process.exit(1)
})

export default app
