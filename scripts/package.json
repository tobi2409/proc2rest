{
  "name": "proc2rest",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "generate-server": "node generate-server.js --appPath=${npm_config_apppath:-../} --generatedDir=${npm_config_generateddir:-generated} --configPath=${npm_config_configpath:-proc2rest.config.json}",
    "generate-client": "node generate-client.js --appPath=${npm_config_apppath:-../} --generatedDir=${npm_config_generateddir:-generated} --configPath=${npm_config_configpath:-proc2rest.config.json}",
    "generate": "node generate-server.js --appPath=${npm_config_apppath:-../} --generatedDir=${npm_config_generateddir:-generated} --configPath=${npm_config_configpath:-proc2rest.config.json} && node generate-client.js --appPath=${npm_config_apppath:-../} --generatedDir=${npm_config_generateddir:-generated} --configPath=${npm_config_configpath:-proc2rest.config.json}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "devDependencies": {
    "ts-morph": "^25.0.0"
  }
}
