{
  "name": "proc2rest",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "generate-server": "node generate-server.js",
    "generate-client": "node generate-client.js",
    "generate": "node generate-server.js --appPath=${npm_config_apppath:-../} && node generate-client.js --appPath=${npm_config_apppath:-../}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "devDependencies": {
    "ts-morph": "^25.0.0"
  }
}
