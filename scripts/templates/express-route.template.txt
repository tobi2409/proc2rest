app.{{expressMethod}}('/api/{{methodName}}', {{routeMiddleware}}, async (req: Request, res: Response) => {
  try {
    const args = {{argsExpression}}

    {{missingParamsBlock}}

    const result = await Promise.resolve({{methodCall}})
    {{sendResultExpression}}
  } catch (error) {
    res.status(500).json({ error: error instanceof Error ? error.message : 'Unknown error' })
  }
})
