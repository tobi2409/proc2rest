// Auto-generated by generate-client.js. Do not edit manually.
const API_BASE_URL = 'http://localhost:3000'

async function request(path, method, params = {}) {
  const isGet = method === 'GET'
  const queryString = new URLSearchParams(
    Object.entries(params ?? {}).filter(([, value]) => value !== undefined).map(([key, value]) => [key, String(value)])
  ).toString()
  const url = isGet
    ? `${API_BASE_URL}${path}${queryString ? `?${queryString}` : ''}`
    : `${API_BASE_URL}${path}`

  const response = await fetch(url, {
    method,
    headers: isGet ? {} : { 'Content-Type': 'application/json' },
    body: isGet ? undefined : JSON.stringify(params)
  })

  const payload = await response.json()

  if (!response.ok) {
    throw new Error(payload?.error ?? `Request failed with status ${response.status}`)
  }

  return payload.result
}

{{methodsBlock}}
